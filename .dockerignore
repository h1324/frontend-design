# Keep the build context small and the builder's `npm ci` clean — never ship the host's
# node_modules or a stale build into the image.
node_modules
.next
.git
.gitignore
npm-debug.log*
*.log
coverage
.env
.env.*
!.env.example
Dockerfile
.dockerignore
docker-compose.yml
README*.md
tests
playwright*
/tmp
scratchpad
