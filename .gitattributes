# Keep shell scripts LF-only — they run inside Linux containers, and a CRLF checkout on Windows
# breaks the shebang ("/usr/bin/env: 'bash\r': No such file or directory").
*.sh text eol=lf
docker/entrypoint.sh text eol=lf
